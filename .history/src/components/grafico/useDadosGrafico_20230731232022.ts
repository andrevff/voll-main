interface IDados {
    nome: string,
    consultas: number
}

const useDadosGrafico = ({ profissionais, consultas }) => {
    let dados: Array<IDados> = []

    if (profissionais && consultas)
}

export default useDadosGrafico